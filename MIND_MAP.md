# Mind Map Format - Self-Documentation

> **For AI Agents:** This mind map is your primary knowledge index. Start with the project anchors [6-10], then follow links [N] to find what you need; consult format nodes [1-5] only when you need the mind-map rules. Always reference node IDs. When you encounter bugs, document your attempts in relevant nodes. When you make changes, update outdated nodes immediately—especially project overview nodes since they're your springboard. Add new nodes only for genuinely new concepts. Keep it compact (20-50 nodes typical). The mind map wraps every task: consult it, rely on it, update it. Never change the first 5 numbered points.

[1] **Mind Map Format Overview** - A graph-based documentation format stored as plain text files where each node is a single line containing an ID, title, and inline references [2]. The format leverages LLM familiarity with citation-style references from academic papers, making it natural to generate and edit [3]. It serves as a superset structure that can represent trees, lists, or any graph topology [4], scaling from small projects (<50 nodes) to complex systems (500+ nodes) [5].

[2] **Node Syntax Structure** - Each node follows the format: `[N] **Node Title** - node text with [N] references inlined` [1]. Nodes are line-oriented, allowing line-by-line loading and editing by AI models [3]. The inline reference syntax `[N]` creates bidirectional navigation between concepts, with links embedded naturally within descriptive text rather than as separate metadata [1][4]. This structure is both machine-parseable and human-readable, supporting grep-based lookups for quick node retrieval [3].

[3] **Technical Advantages** - The format enables line-by-line overwriting of nodes without complex parsing [2], making incremental updates efficient for both humans and AI agents [1]. Grep operations allow instant node lookup by ID or keyword without loading the entire file [2]. The text-based storage ensures version control compatibility, diff-friendly editing, and zero tooling dependencies [4]. LLMs generate this format naturally because citation syntax `[N]` mirrors academic paper references they've seen extensively during training [1][5].

[4] **Graph Topology Benefits** - Unlike hierarchical trees or linear lists, the graph structure allows many-to-many relationships between concepts [1]. Any node can reference any other node, creating knowledge clusters around related topics [2][3]. The format accommodates cyclic references for concepts that mutually depend on each other, captures cross-cutting concerns that span multiple subsystems, and supports progressive refinement where nodes are added to densify understanding [5]. This flexibility makes it suitable as a universal knowledge representation format [1].

[5] **Scalability and Usage Patterns** - Small projects typically need fewer than 50 nodes to capture core architecture, data flow, and key implementations [1]. Complex topics or large codebases can scale to 500+ nodes by adding specialized deep-dive nodes for algorithms, optimizations, and subsystems [4]. The methodology includes a bootstrap prompt for generating initial mind maps from existing codebases automatically [1]. Scale is managed through overview nodes [1-5] that serve as navigation hubs, with detail nodes forming clusters around major concepts [3][4]. The format remains navigable at any scale due to inline linking and grep-based search [2][3].
[6] **Project Overview** - AWS Billing Simulator is a local-first single-user training app for AWS billing, Organizations, cost allocation, Cost Explorer-style analysis, budgets, bills, invoices, payments, exports, and scenario labs without real AWS accounts, credentials, payment methods, or spend [7][8][27]. The proposal remains in `aws-billing-simulator-proposal.md`; current implemented behavior is summarized in `README.md` and this compact index [24][25][27].
[7] **Architecture and Stack** - The app is one Go binary, `billing-sim`, serving a local browser UI on loopback with embedded templates/assets and SQLite workspace databases [6][10][13]. There is no required Node.js, npm, React, PostgreSQL, Electron, hosted service, AWS credential, or real payment integration for core use [10][28].
[8] **Product Domains** - Core domains are local app/workspace lifecycle [13], organization simulation and billing visibility [17], resource usage and pricing [18], billing pipeline and close [19], bills and invoices [20], scenarios and seed data [21], tags/cost categories/allocation [22][23], Cost Explorer reports [24], budgets and forecasts [25], and future exports/payments/assessment work [26].
[9] **Tracker and Version Control Workflow** - This repo uses `br` for issues and Jujutsu `jj` for version control; never use git commands here [11][12]. Start sessions with `bv --robot-next` for context, but treat legacy `bd` command fields as hints and run `br show`, `br update`, and `br close` with the returned issue ID [12]. Use `br ready` for live entry points and `br ready --label <label>` for label-scoped user requests; `br stats` may show stale `bd list` guidance and unreliable recent-activity counters, so use `br list`, `br ready`, `br show`, and `br count` for actionable facts [11][12].
[10] **Implementation Constraints** - Keep core workflows deterministic, local, and inspectable in a fresh workspace [6][7]. SQLite uses WAL mode, foreign keys, busy timeouts, short write transactions, embedded migrations, durable line items as billing source of truth, explicit summary tables for reporting, and deterministic scenario/test data [16][18][19][21].
[11] **Session Completion Checklist** - Work one issue at a time, create focused tests when code or behavior changes, file follow-up issues for remaining work, run relevant quality gates, run `br sync --flush-only` before committing, describe the Jujutsu change with `jj desc -m "<description>"`, then `jj new` after the completed change is committed [9][12][14]. For docs-only mind-map edits, inspect structure and references, run lightweight text checks, close the tracker item, and avoid broad Go gates unless code changed [12][30].
[12] **Current Work Pointers** - Live backlog state wins over historical notes [9]. As of this consolidation, closed foundations include local app foundation `bd-2ee`, organization/account simulation `bd-371`, usage/pricing/billing engine `bd-zaw`, tags/cost-categories/allocation `bd-2rx`, Cost Explorer query/report UI through `bd-1of.1` and `bd-1of.2`, budgets/forecasting feature `bd-1of.3` with children `bd-1of.3.1`, `bd-1of.3.2`, and `bd-1of.3.3`, bills page `bd-29f.1`, invoices `bd-29f.2`, and scenario DSL foundations `bd-19f.1` [13][17][19][20][21][22][23][24][25]. Ready work should be selected from `br ready`, not from embedded counts [9][26].
[13] **Local App and Workspace Shell** - The CLI, config, logging, local server lifecycle, `/healthz`, workspace selector, remembered workspace state, browser launch, graceful close, shared navigation, reusable server-rendered partials, embedded assets, and smoke-tested packaging are implemented [7][11]. Agents should pass project-local `-workspace ./tmp/workspace`, `-state ./tmp/state.json`, and `GOCACHE="$PWD/tmp/go-build-cache"` for local runs [28].
[14] **Agent Mistakes and Prevention** - Consolidated recurring lessons: derive file paths from `rg --files` before opening guessed names; read exact large-file anchors before broad patches; quote shell patterns that contain backticks; quote multi-word `jj desc -m` messages; use repo-local `GOCACHE` for Go validation; sequence formatters and write commands before read checks; verify actual files when `jj diff` output looks like adjacent-line artifacts; check `br --help`, `br show`, or `br dep tree` before assuming unsupported tracker flags; check the repo tree before including absent `test/` or `features/` paths in broad searches [9][11][28]. 2026-06-06 `bd-2mx`: no new tool or implementation mistake; the cleanup intentionally collapsed repeated no-mistake notes into this single prevention node [30].
[15] **User Preferences** - The user repeatedly asks agents to follow `README.md`, `AGENTS.md`, and `MIND_MAP.md`, work through one beads issue at a time, and keep mistake/preference notes concise and actionable [9][11][14]. The user asked for fresh-eyes reviews to file concrete follow-up tickets with the `quality` label, prefers workspace-local temporary files only, and on 2026-06-06 again asked to start from README/AGENTS/MIND_MAP before the next ticket [29][30].
[16] **Persistence Foundation** - `internal/persistence` owns workspace opening, migrations, metadata repositories, transactions, WAL/foreign-key/busy-timeout pragmas, and migration history validation [10]. Migrations currently cover workspace metadata, price catalog, resources/usage, metering, bill line items, simulator clock, daily jobs, closes, support charges, invoices, organizations, billing visibility helpers, tags, Cost Categories, split charges, Cost Explorer summaries, budgets, and forecasts [18][19][20][22][23][24][25].
[17] **Organization and Billing Visibility** - The AnyCompany Retail seed organization includes management account, OUs, member accounts, suspended/closed lifecycle examples, account tags, reset-from-template behavior, and organization tree/lifecycle UI [8][21]. Billing visibility models management, finance, instructor, and member-account viewers across bills, invoices, saved Cost Explorer reports, and account-scoped navigation [20][24].
[18] **Resources, Usage, and Price Catalog** - The synthetic price catalog has deterministic USD rates and lookup/unit conversion logic for EC2, EBS, S3, Lambda, RDS, NAT Gateway, CloudWatch Logs, data transfer, Support, and Marketplace examples [10][19]. The Resources lab creates resources, manages active tags, records usage, generates deterministic usage patterns, snapshots usage-window tags, validates resource lifecycle windows, and exposes pricing estimates without JavaScript package tooling [7][22].
[19] **Billing Pipeline and Period Close** - Usage events become metering records, priced bill line items, service summaries, Support fee rows, month-end closes, issued bills, invoice obligations, invoice documents, and finalized closed-period records [10][16][18][20]. The simulator clock drives daily metering and period boundaries; cross-period billable windows, closed-period mutation, payer mismatches, and finalized Cost Category/split allocations are guarded by repository validation and SQLite triggers [23][25].
[20] **Bills and Invoices** - `/bills` renders bill state summaries, charge breakdowns by service/account/region/usage type, resource drilldowns, and bill-to-line-item reconciliation [19]. Invoice documents are persisted at close, printable invoice pages are server-rendered, CSV exports expose line items, and the PDF route currently returns an implementation plan rather than a generated PDF artifact [17][26].
[21] **Scenarios and Seeds** - `internal/scenario` parses scenario JSON, validates errors with useful context, executes deterministic setup events, records scenario audits, and resets the organization template before scenario application while preserving unrelated workspace artifacts [8][17]. Packaged seed data includes AnyCompany Retail and the "Find the untagged data-transfer spike" lab [18][22].
[22] **Cost Allocation Tags** - Cost allocation tags have discovered key/value inventory, activation/deactivation history, 24-hour pending visibility timing, spend/resource/account/service coverage, untagged spend, case-mismatch detection, and usage-window snapshot behavior [18][24]. `/tags` exposes the lifecycle and coverage workflow with server-rendered forms and tables [7].
[23] **Cost Categories and Split Charges** - Cost Categories support ordered rules over account, service, region, usage type, line item type, tag, and existing-category conditions; preview shows rule effects and shadowing, and persisted line-item assignment snapshots are protected after close [19][22]. Split-charge rules support even, fixed-share, and proportional allocation with deterministic rounding, source-line audit rows, allocation comparison, residual reporting, and finalized-period preservation [24].
[24] **Cost Explorer Reports** - Cost Explorer query execution aggregates bill line items by hourly, daily, or monthly buckets with dimension/tag/Cost Category filters and up to two groupings [19][22][23]. `/cost-explorer` includes saved report selection/create/update, line/bar/stacked-bar SVG charts, dense result tables, source line-item drilldowns, aggregate CSV export, and summary tables for daily cost, monthly account/service spend, tag coverage, and Cost Category rollups [25].
[25] **Budgets and Forecasts** - Monthly budgets can be scoped by linked account, service, tag, or Cost Category and evaluated against actual and forecast thresholds [22][23][24]. Forecast summaries persist active budget projections using actual spend, elapsed-day run rate, and priced future scenario/generator usage events; breached threshold checks create idempotent in-app alert notification history on `/budgets` without sending real email [26].
[26] **Open Product Areas** - Remaining planned areas include payment methods/profiles/state workflows, past-due remediation labs, CUR-like and FOCUS-style exports, local query lab, instructor-authored scenarios and grading, payment/invoice refinements, and deferred advanced billing fidelity such as credits, taxes, Savings Plans, Reserved Instances, blended rates, and amortized views [8][12][20][25].
[27] **README and Public Contract** - `README.md` is the public summary of what the simulator is for, what it currently implements, planned product areas, architecture, local run instructions, development gates, issue-tracker conventions, and safety boundaries [6][7][11]. Keep README current when user-visible functionality moves between planned and implemented states [24][25][26].
[28] **Agent-Safe Local State** - Never touch files outside the project directory [15]. Use project-local `tmp/` for workspaces, logs, generated files, demos, app state, and Go build cache; run local app smoke checks with `GOCACHE="$PWD/tmp/go-build-cache" go run ./cmd/billing-sim -http 127.0.0.1:8080 -workspace ./tmp/workspace -state ./tmp/state.json -browser=false` [13][11].
[29] **Quality Review Practice** - For review requests, lead with concrete findings, file actionable follow-up issues when appropriate, and prefer verified defects or missing coverage over speculative cleanup [15]. For label-scoped quality work, run `br ready --label quality` before generic recommendations, and use `br create --labels quality` for newly found follow-ups [9][11].
[30] **Mind-Map Maintenance** - `bd-2mx` consolidated `MIND_MAP.md` from a long per-ticket session diary into this compact node set while preserving protected format nodes [1]-[5], restoring missing agent-mistake coverage at [14], and aligning project/workflow anchors with `README.md` and `AGENTS.md` [6][9][15][27]. Future updates should modify the closest existing node, add a new node only for a genuinely new concept, and avoid repeating issue IDs or "no new mistake" notes across many nodes [1][5][14].
