# Mind Map Format - Self-Documentation

> **For AI Agents:** This mind map is your primary knowledge index. Start with the project anchors [6-10], then follow links [N] to find what you need; consult format nodes [1-5] only when you need the mind-map rules. Always reference node IDs. When you encounter bugs, document your attempts in relevant nodes. When you make changes, update outdated nodes immediately—especially project overview nodes since they're your springboard. Add new nodes only for genuinely new concepts. Keep it compact (20-50 nodes typical). The mind map wraps every task: consult it, rely on it, update it. Never change the first 5 numbered points.

[1] **Mind Map Format Overview** - A graph-based documentation format stored as plain text files where each node is a single line containing an ID, title, and inline references [2]. The format leverages LLM familiarity with citation-style references from academic papers, making it natural to generate and edit [3]. It serves as a superset structure that can represent trees, lists, or any graph topology [4], scaling from small projects (<50 nodes) to complex systems (500+ nodes) [5].

[2] **Node Syntax Structure** - Each node follows the format: `[N] **Node Title** - node text with [N] references inlined` [1]. Nodes are line-oriented, allowing line-by-line loading and editing by AI models [3]. The inline reference syntax `[N]` creates bidirectional navigation between concepts, with links embedded naturally within descriptive text rather than as separate metadata [1][4]. This structure is both machine-parseable and human-readable, supporting grep-based lookups for quick node retrieval [3].

[3] **Technical Advantages** - The format enables line-by-line overwriting of nodes without complex parsing [2], making incremental updates efficient for both humans and AI agents [1]. Grep operations allow instant node lookup by ID or keyword without loading the entire file [2]. The text-based storage ensures version control compatibility, diff-friendly editing, and zero tooling dependencies [4]. LLMs generate this format naturally because citation syntax `[N]` mirrors academic paper references they've seen extensively during training [1][5].

[4] **Graph Topology Benefits** - Unlike hierarchical trees or linear lists, the graph structure allows many-to-many relationships between concepts [1]. Any node can reference any other node, creating knowledge clusters around related topics [2][3]. The format accommodates cyclic references for concepts that mutually depend on each other, captures cross-cutting concerns that span multiple subsystems, and supports progressive refinement where nodes are added to densify understanding [5]. This flexibility makes it suitable as a universal knowledge representation format [1].

[5] **Scalability and Usage Patterns** - Small projects typically need fewer than 50 nodes to capture core architecture, data flow, and key implementations [1]. Complex topics or large codebases can scale to 500+ nodes by adding specialized deep-dive nodes for algorithms, optimizations, and subsystems [4]. The methodology includes a bootstrap prompt for generating initial mind maps from existing codebases automatically [1]. Scale is managed through overview nodes [1-5] that serve as navigation hubs, with detail nodes forming clusters around major concepts [3][4]. The format remains navigable at any scale due to inline linking and grep-based search [2][3].
[6] **Project Overview** - AWS Billing Simulator is a local-first single-user training app for AWS billing, Organizations, cost allocation, Cost Explorer-style analysis, budgets, bills, invoices, payments, exports, and scenario labs without real AWS accounts, credentials, payment methods, or spend [7][8][27]. The proposal remains in `aws-billing-simulator-proposal.md`; current implemented behavior is summarized in `README.md` and this compact index [24][25][27].
[7] **Architecture and Stack** - The app is one Go binary, `billing-sim`, serving a local browser UI on loopback with embedded templates/assets and SQLite workspace databases [6][10][13]. There is no required Node.js, npm, React, PostgreSQL, Electron, hosted service, AWS credential, or real payment integration for core use [10][28].
[8] **Product Domains** - Core domains are local app/workspace lifecycle [13], organization simulation and billing visibility [17], resource usage and pricing [18], billing pipeline and close [19], bills/invoices/payments [20], scenarios and seed data with learner assessment feedback [21], tags/cost categories/allocation and pro forma showback [22][23], Cost Explorer reports and anomaly alerts [24], budgets and forecasts [25], and CUR-like exports/query-lab workflows [26][31]. Remaining future work is tracked separately [26].
[9] **Tracker and Version Control Workflow** - This repo uses `br` for issues and Jujutsu `jj` for version control; never use git commands here [11][12]. Start by reading any project context files the user explicitly names; for label-scoped requests, use `br ready --label <label>` before generic `bv --robot-next` recommendations, while unscoped requests can use `bv --robot-next` as session-start context [15][29]. Treat legacy `bd` command fields from robot output as hints only and run `br show`, `br update`, and `br close` with the returned issue ID [12]. Use `br ready` for live entry points; `br stats` may show stale `bd list` guidance and unreliable recent-activity counters, so use `br list`, `br ready`, `br show`, and `br count` for actionable facts [11][12].
[10] **Implementation Constraints** - Keep core workflows deterministic, local, and inspectable in a fresh workspace [6][7]. SQLite uses WAL mode, foreign keys, busy timeouts, short write transactions, embedded migrations, durable line items as billing source of truth, explicit summary tables for reporting, and deterministic scenario/test data [16][18][19][21].
[11] **Session Completion Checklist** - Work one issue at a time, create focused tests when code or behavior changes, file follow-up issues for remaining work, run relevant quality gates, run `br sync --flush-only` before committing, describe the Jujutsu change with `jj desc -m "<description>"`, then `jj new` after the completed change is committed [9][12][14]. When `br ready` lists a parent and an open child, verify and close the child issue in its own lifecycle before moving up to the parent so success criteria remain honest. For docs-only mind-map edits, inspect structure and references, run lightweight text checks, close the tracker item, and avoid broad Go gates unless code changed [12][30].
[12] **Current Work Pointers** - Live tracker state is the source of truth for current and closed work: use `br ready`, `br ready --label <label>`, `br show <id>`, `br list`, and `br count` instead of relying on issue ledgers embedded in this file [9][11][15]. This node should stay a navigation hub, not a complete backlog archive; use the topical nodes for implemented product areas: local app shell [13], organization and visibility [17], resources and pricing [18], billing close [19], bills/invoices/payments [20], scenarios [21], tags [22], Cost Categories and split charges [23], Cost Explorer [24], budgets [25], exports/query lab [26][31], and public-contract docs [27]. Historical issue IDs may appear in nearby nodes only when they identify a durable milestone or dated example; reusable quality lessons belong in [14] and [29], and mind-map maintenance rules belong in [30]. Ready work should be selected from live `br` output, not embedded counts or stale closed-issue lists [9][26].
[13] **Local App and Workspace Shell** - The CLI, config, logging, local server lifecycle, `/healthz` with GET/HEAD-only method guard, workspace selector, generated fresh-workspace start action, learner overview intro page, remembered workspace state, browser launch, graceful close, shared navigation, reusable server-rendered partials, embedded assets, and smoke-tested packaging are implemented [7][11]. DB-backed workspace routes hold request leases so workspace open/reset/clone/close swaps wait before closing or replacing the active SQLite handle; scenario seed reset stages current SQLite files, restores them on cleanup/open/state-save failures, and reopens the workspace so failed resets do not detach the session. Agents should pass project-local `-workspace ./tmp/workspace`, `-state ./tmp/state.json`, and `GOCACHE="$PWD/tmp/go-build-cache"` for local runs [28].
[14] **Agent Mistakes and Prevention** - Reusable prevention rules: use `cs` first for broad codebase exploration, concept searches, likely implementation areas, and declarations when available, using `--color never` on this install when plain output is needed, then use `rg --files` and `rg -n` to confirm exact paths, exhaustive usages, and line-numbered evidence before editing; derive paths from `rg --files` before opening or searching guessed locations, and do that path check before any multi-root `rg`; scope broad searches to existing roots and re-run `rg --files` when unsure so nonexistent `docs`/`test`/`features` dirs do not add noisy partial-output errors; for docs-only verification, search exact files or source anchors first so broad code-tree searches do not produce truncated, low-signal output; do not include conventional roots or guessed package roots like `test` or `scenario` in `rg` commands unless `rg --files` confirms they exist, including later broad-search attempts after the file list is known; quote shell patterns with backticks, braces, or globs, and use single quotes around any pattern containing backticks or `rg -F` with shell-safe quoting because double quotes still allow backtick command substitution in zsh; use ANSI-C quoting or a temp file for multiline tracker descriptions so literal `\n` text is not stored; avoid command chaining and shell pipes such as `| head` when independent reads can run through `multi_tool_use.parallel` or tool output limits; after the `bd-11mq` split, remember that `rg --files ... | rg` repeats the same mistake, so use `rg --files <root>` alone or a separate `rg -n` confirmation command; keep dependent verification commands such as formatters, tests, and diffs serial, especially `gofmt` before `go test` or `jj diff`; do not run formatters in parallel with tests/grep/diff verification because verification can inspect pre-format content; read exact anchors before patching large single-line docs, repeated setup blocks, or common return-value regions; inspect actual file snippets when `jj diff` looks like adjacent-line noise; keep label-scoped work ordering consistent across AGENTS and this mind map so agents read named docs first, resolve `br ready --label <label>` before generic robot recommendations, run `br show <id>` serially before `br update <id> --status in_progress`, skip robot-recommended issues that are deferred or absent from ready output, and never run legacy robot `bd` commands; confirm helper scope, table names, route registration paths, tracker flags, exported units, migration-count/user_version assertions, and exact defect behavior with local code or tests before filing or writing changes; after helper refactors, remove stale imports found by the first focused compile/test failure before broad gates; when writing cross-scope export regressions, make the comparison request include dimensions normally injected by policy such as `usage_account_id`, otherwise the test may exercise an all-account path by mistake; do not assume tracker flags exist: this install uses `br create --labels` but `br list --label`, and unsupported `br list --parent` or `br count --label` style filters may fail, so use CLI help, `br show` dependencies, or supported flags before acting; test HEAD handlers that must not write bodies through `httptest.ResponseRecorder` or direct mux calls because `httptest.Server`/`net/http` can suppress wire bodies even when handlers wrote bytes; keep workspace-opening app tests serial unless runtime is proven cheap; for workspace swap/reset recovery, cover cleanup, replacement-open, and state-save failures with direct non-parallel session tests so a cleared handle cannot hide behind HTTP redirect behavior; use repo-local `GOCACHE`; normalize app HTTP test helpers because `appTestHTTPClient` returns an `http.Client` value while `httptest.Server.Client()` returns `*http.Client`; start smoke servers with `tty=true` or a planned shutdown path; classify workspace swap endpoints outside request-lease middleware or they can self-deadlock when calling open/reset/clone write locks; remember CUR CSV numeric cost/rate/usage fields are decimal strings, not micros, when writing learner SQL examples; when centralizing viewer-policy helpers, reject account IDs without explicit roles before applying default-management behavior and keep Bills row visibility separate from invoice-document permission [9][11][20][28][30]. Recent examples worth retaining: payment profiles must respect organization reset semantics [17][20]; scenario bills can include Support rows beyond direct usage [19][21]; scenario reset should recreate a workspace when seeds close billing periods [19][21]; packaged scenario/check tests should validate authored expectations against seed data instead of assuming labels stayed correct [21]; `gofmt` and `jj diff` must not run in parallel because the diff can inspect stale formatting state; after `bd-1r6.4.1`, keep formatter and test reruns as separate commands instead of using `&&`; when repeated multi-file `apply_patch` hunks fail, split file additions from mechanical replacements and verify exact matches with `rg` before retrying [31]; during `bd-1r6.4.2`, guessed template/helper filenames and a nonexistent `templates` root repeated the same path-discovery mistake, so use symbol lookup or `rg --files` before opening likely filenames; during `bd-1r6.3.1`, guessed `internal/persistence/test_helpers_test.go` instead of opening the exact helper path from `rg`, so treat helper files like any other path-discovery target; during `bd-1r6.2.2`, I repeated the no-pipe/serial-verification mistakes with `rg --files ... | sort` and `gofmt` beside `jj status`, so treat status/listing commands as verification when a formatter is running. This node should store durable mistake patterns, not a per-ticket chronology [15][30].
[15] **User Preferences** - The user repeatedly asks agents to start by reading `README.md`, `AGENTS.md`, and `MIND_MAP.md`; when those filenames appear in the prompt, treat them as required first-read context, then follow the `br`/`jj` one-issue lifecycle and select label-scoped work from `br ready --label <label>` before generic recommendations [9][11][29]. The user wants `AGENTS.md` to recommend `cs` as the first codebase search pass, with `rg` retained for exact confirmation before edits [14]. Treat "work hard on the next ticket with quality label" as a complete issue lifecycle: claim the first concrete ready `quality` item, implement or document it thoroughly, verify appropriately, sync and describe the jj change, close the issue, then `jj new` [11][29]. When the user asks for fresh-eyes review, file concrete verified follow-up tickets with the `quality` label rather than switching to roadmap work. When the user asks to continue from a proposed ticket, proceed with that selected issue and file follow-up tickets only for concrete work discovered during implementation; when the user asks to consider deferred tickets and then says "start", promote and work the recommended deferred issue rather than waiting for another issue ID. Preserve first-class "Start New Experience" language for fresh workspace creation when the user asks to start from scratch. For public GitHub publishing, keep AWS trademark/non-affiliation disclaimers in prominent README and browser UI locations. The user prefers workspace-local temporary files only, concrete follow-up tickets over speculative notes, and concise actionable mind-map entries instead of per-session diary accumulation [14][28][30].
[16] **Persistence Foundation** - `internal/persistence` owns workspace opening, migrations, metadata repositories, transactions, WAL/foreign-key/busy-timeout pragmas, and migration history validation [10]. Migrations currently cover workspace metadata, price catalog items and manifests, resources/usage, metering, bill line items, simulator clock, daily jobs, closes, support charges, Reserved Instance purchases/generated source links, Savings Plan purchases/generated source links, invoices, organizations, billing visibility helpers, tags, Cost Categories, split charges, Cost Explorer summaries, budgets, forecasts, cost anomaly alerts, pro forma billing groups/plans/generated line items/custom line items, seller-of-record profiles, payer payment profiles, payment methods, invoice payment lifecycle state/events, and workspace export file metadata [18][19][20][22][23][24][25].
[17] **Organization and Billing Visibility** - The AnyCompany Retail seed organization includes management account, OUs, member accounts, a suspended Deprecated Prototype account, account tags, reset-from-template behavior, and organization tree/lifecycle UI for creating, moving, suspending, and closing accounts [8][21]. Its migration-created seed rows and Go reset template are guarded by `TestAnyCompanyRetailMigrationSeedMatchesResetTemplate`, which compares organization header, OUs, accounts, lifecycle baseline fields, and account tags; seed changes should update both paths and add a forward migration for existing workspaces when needed [16][29]. Billing visibility models management, finance, instructor, and member-account viewers across bills, invoices, saved Cost Explorer reports, and account-scoped navigation [20][24].
[18] **Resources, Usage, and Price Catalog** - The synthetic price catalog has deterministic USD rates, catalog-level source/fetch/effective metadata, supported-region manifests, scenario-run compatibility snapshots, and lookup/unit conversion logic for EC2, EBS, S3, Lambda, RDS, NAT Gateway, CloudWatch Logs, data transfer, Support, and Marketplace examples [10][19][21]. The Resources lab creates resources, manages active tags, records usage, generates deterministic usage patterns, snapshots usage-window tags, validates resource lifecycle windows, and exposes pricing estimates without JavaScript package tooling [7][22]. Resource Lab code keeps inventory and usage forms/views in resource-focused files while `internal/app/resource_billing_workflows.go` and `internal/app/resource_billing_views.go` own clock, pricing, metering, close, issued bill, and price-dimension workflow assembly [19][29].
[19] **Billing Pipeline and Period Close** - Usage events become metering records, priced bill line items, basic EC2 Reserved Instance upfront/recurring Fee rows plus coverage Credit rows, basic Compute Savings Plan upfront/recurring commitment Fee rows plus negation Credit rows, service summaries, Support fee rows, month-end closes, issued bills, invoice obligations, invoice documents, and finalized closed-period records [10][16][18][20]. Reserved Instance coverage is persistence-first: owner-account matching usage is covered before shared payer-account usage, and coverage is capped by purchase term overlap; Savings Plan negations run after RI coverage, cover remaining matching EC2 RunInstances hourly spend up to the hourly commitment, and persist covered quantity/cost plus simple amortized commitment cost on source links for later metrics [26]. The simulator clock drives daily metering and period boundaries; cross-period billable windows, closed-period mutation, payer mismatches, and finalized Cost Category/split allocations are guarded by repository validation and SQLite triggers [23][25].
[20] **Bills, Invoices, and Payments** - `/bills` renders bill state summaries, charge breakdowns by service/account/region/usage type, resource drilldowns, and bill-to-line-item reconciliation with complete bill-state cards and capped table labels such as `N of M shown` when summary/reconciliation/breakdown displays hit their fixed UI limits [19][29]. Invoice documents are persisted at close, snapshot the payer's active default payment profile seller/bill-to fields, printable HTML invoice pages are server-rendered, CSV exports expose line items, and `/invoices/{invoice_id}/document.pdf` serves bundled synthetic `application/pdf` invoice downloads using the same printable invoice read model [17][26]. Scoped Bills invoice HTML/CSV/PDF links preserve `viewer_role`/`viewer_account_id`, invoice page CSV/PDF/Bills actions and PDF links preserve the same viewer scope, invoice routes deny member viewers plus payer-scoped financial viewers whose policy cannot see the invoice payer, and member-scoped Bills summaries show restricted invoice state while preserving scoped CUR CSV/reconciliation actions [17][29]. `/payments` applies `viewer_role`/`viewer_account_id` payment policy, allowing management, finance, and instructor payment management while denying member viewers, filtering payer-scoped rows, and preserving viewer scope through invoice links, action forms, and redirects; it shows due invoices, payment history, failed reasons, retry/collect/past-due actions, unapplied Advance Pay balances, and method fix/default actions on top of payment lifecycle persistence for due, scheduled, processing, succeeded, failed, past-due, partially paid, and refunded transitions [25][26][29]. Payment POST commands live in `internal/app/payment_actions.go`, where the action registry owns submitted command lookup, form parsing, POST payment-policy resolution, payer visibility checks, repository mutation calls, and flash messages, while `payment_handlers.go` keeps page rendering and read-side loading [29]. Partial collections against a past-due obligation keep the bill visible as `past_due` while the invoice state is `partially_paid` until the remaining balance succeeds or is explicitly marked due [29].
[21] **Scenarios and Seeds** - `internal/scenario` parses scenario JSON plus local block-style YAML drafts, validates errors with useful context, executes deterministic setup events, records scenario audits, persists learner progress/actions/check results, evaluates assessment checks for owner-role-scoped saved reports, top cost drivers, tag activation, Cost Category rules, bill reconciliation, and payment status, and resets the organization template before scenario application while preserving unrelated workspace artifacts [8][17][20][22][23][24]. Scenario event actions are registered in `internal/scenario/event_actions.go`, where each action owns normalization, validation, semantic/preflight hooks, and runner execution; keep future action additions in that registry and its coverage test [14][29]. Scenario runner orchestration is split so `runner.go` owns the high-level `Run` flow, `runner_preflight.go` owns closed-period preflight/conflict lookup, and `runner_orchestration.go` owns run audit plus learner-progress persistence helpers [14][29]. Scenario launches now preflight definition-known pricing into already closed payer billing periods after run/progress audit creation but before organization reset or resource/usage mutation, and any remaining closed-period trigger failure is translated into a payer/period reset-or-clone message [19][29]. `/scenarios` lists packaged labs with phase/objective/estimated duration, launches seeds through POST `/scenarios/launch`, shows recent run audit and progress state, links resume actions into Bills, Tags, Cost Explorer, Cost Categories, Budgets, or Payments, and now exposes feedback reports, reset-to-seed, clone workspace, and review archive actions for completed runs [13][20][22][24][25]. `/scenarios/feedback?scenario_run_id=...` renders learner-facing action/check evidence with what changed, source tables, and AWS billing concept mappings [13]. `/scenarios/editor` provides a local YAML draft editor with validation preview through POST `/scenarios/editor/validate`; previewing does not launch scenarios or write scenario run audit rows [13]. Reset replaces the current `simulator.db` before applying the seed; clone checkpoints WAL and copies the workspace to a new empty directory; archive writes `review-archives/*.zip` with `manifest.json`, `feedback-report.json`, `workspace/simulator.db`, per-bill CUR CSV, and reconciliation JSON, and its manifest stores a display-safe workspace label instead of the creator's local path [19][24][28][29]. Packaged seed data includes AnyCompany Retail, the "First consolidated bill" lab with scenario account creation and management-payer invoice close, the "Missing Tags" lab with tag discovery/activation, delayed visibility, case-mismatched owner tags, and untagged spend coverage, the "Shared Networking allocation" lab with product-team spend, central NAT/data-transfer source spend, a Product Cost Category, fixed 60/40 split-charge allocation, the "Payment Failure" lab with default-card setup, invoice close, failed collection history, and due-state retry, the "Forecast and Budget Alert" lab with mid-month Storefront spend, future scheduled usage, a forecast budget breach, alert notification, and saved Cost Explorer drilldown report, and the "Find the untagged data-transfer spike" lab [18][19][20][22][23][25].
[22] **Cost Allocation Tags** - Cost allocation tags have discovered key/value inventory, activation/deactivation history, 24-hour pending visibility timing, spend/resource/account/service coverage, untagged spend, case-mismatch detection, and usage-window snapshot behavior [18][24]. `/tags` exposes the lifecycle and coverage workflow with server-rendered forms and tables; discovery refresh is explicit POST `/tags/refresh`, while GET `/tags` renders persisted state only [7][29].
[23] **Cost Categories, Split Charges, and Pro Forma** - Cost Categories support ordered rules over account, service, region, usage type, line item type, tag, and existing-category conditions; preview shows rule effects and shadowing, and persisted line-item assignment snapshots are protected after close [19][22]. Persistence code is split so `cost_categories.go` keeps category/rule CRUD, `cost_category_preview.go` owns preview read models, `cost_category_assignment_store.go` owns assignment refresh/list storage plus summary refresh coupling, and `cost_category_evaluator.go` owns pure recursive rule evaluation with cycle detection. Split-charge rules support even, fixed-share, and proportional allocation with deterministic rounding, source-line audit rows, allocation comparison, residual reporting, and finalized-period preservation [24]. `/pro-forma` supports Billing Conductor-style internal showback with pricing plans, service-level rate multipliers, billing groups, account assignment, generated pro forma rows, custom fee/markup/credit/annotation rows, and source bill-line references that keep payable bill line items unchanged [19][27].
[24] **Cost Explorer Reports** - Cost Explorer query execution aggregates bill line items by hourly, daily, or monthly buckets with dimension/tag/Cost Category filters and up to two groupings [19][22][23]. `/cost-explorer` includes owner role/account-scoped saved report selection/create/update/run workflows with role-aware same-name shelves, line/bar/stacked-bar SVG charts, dense result tables, source line-item drilldowns, aggregate CSV export, and summary tables for daily cost, monthly account/service spend, tag coverage, and Cost Category rollups [17][25][29]. `/anomalies` compares a selected current period with a baseline period through explicit POST `/anomalies/refresh`, then persists reviewable service, account, tag, and Cost Category spend-spike alerts while GET renders stored alerts without mutation [16][22][23][29]. Drilldown pages display the first bounded page of source line items but compute line-item count, usage quantity, and unblended cost cards from complete matching totals and label capped displays as `N of M items shown` [20][29]. Cost Explorer page results, CSV exports, drilldowns, and saved-report run metadata now apply the selected owner role/account through the billing visibility policy, so member owners see only their usage-account rows while management/finance/instructor keep their broader scopes [17][29].
[25] **Budgets and Forecasts** - Monthly budgets can be scoped by linked account, service, tag, or Cost Category and evaluated against actual and forecast thresholds [22][23][24]. Forecast summaries persist active budget projections using actual spend, elapsed-day run rate, and priced future scenario/generator usage events; breached threshold checks create idempotent in-app alert notification history through explicit POST `/budgets/refresh`, while GET `/budgets` reads persisted budget, forecast, and alert state without sending real email or mutating generated rows [26].
[26] **Open Product Areas** - Remaining planned areas include richer past-due payment remediation teaching workflows beyond current retry/fix flows, strict FOCUS data conformance and richer FOCUS coverage beyond the current FOCUS-like CSV export, richer query-lab workflows beyond external SQL examples, richer instructor-authored scenarios, instructor assessment review workflows beyond learner progress/check feedback, payment/invoice refinements, richer pro forma showback labs, and deferred advanced billing fidelity such as credits, taxes, richer Savings Plan and Reserved Instance scenarios/UI, blended rates, and amortized report views [8][12][20][21][23][25][27]. The DuckDB decision for `bd-hz2.2.1` keeps DuckDB optional/external rather than embedded in the simulator binary unless later in-app SQL requirements justify the packaging tradeoff; `/query-lab` examples target generated CUR CSV exports, accept selected generated export filenames or explicit CSV paths, and preserve the export boundary.
[27] **README and Public Contract** - `README.md` is the public summary of what the simulator is for, what it currently implements, planned product areas, architecture, local run instructions, development gates, issue-tracker conventions, and safety boundaries [6][7][11]. Keep README current when user-visible functionality moves between planned and implemented states; it now lists path-based workspace opening, generated fresh workspace starts from Workspaces and Overview, the learner overview intro page, packaged Shared Networking allocation, Payment Failure, and Forecast and Budget Alert lab support, scenario reset/clone/archive actions, local scenario editor validation preview, learner progress/check-result persistence, scenario feedback reports, printable HTML invoices plus bundled PDF and CSV invoice downloads, basic EC2 Reserved Instance fee/coverage persistence, basic Compute Savings Plan commitment fee/negation persistence with amortized source links, Billing Conductor-style pro forma pricing plans/billing groups/showback rows/custom rows, Cost Explorer report builder and anomaly alert workflows, payer-period CUR-like and FOCUS-like CSV downloads with FOCUS v1.4 target metadata sidecars, workspace-local export file metadata, Exports UI explicit generation/list/download/regeneration workflow, export reconciliation reports with scoped totals, Query Lab external SQL examples over generated or selected CUR CSV paths, the FOCUS-like schema note in `docs/focus-like-export-schema.md`, AWS trademark/non-affiliation disclaimers in README plus the shared app footer, and the query-lab decision to keep DuckDB optional/external for this phase [13][19][20][21][23][24][25][26][31].
[28] **Agent-Safe Local State** - Never touch files outside the project directory [15]. Use project-local `tmp/` for workspaces, logs, generated files, demos, app state, and Go build cache; run local app smoke checks with `GOCACHE="$PWD/tmp/go-build-cache" go run ./cmd/billing-sim -http 127.0.0.1:8080 -workspace ./tmp/workspace -state ./tmp/state.json -browser=false` [13][11].
[29] **Quality Review Practice** - For review requests, lead with concrete findings, verify defects before filing follow-ups, and create actionable `quality` issues for remaining work [15]. For label-scoped quality work, select from `br ready --label quality` before `bv --robot-next`; if that queue is empty, do focused review and file a verified quality issue rather than switching to unrelated ready work [9][11]. Effective quality fixes in this repo have targeted state mutation on GET, persisted generation behind explicit POST actions, billing-visibility leaks, cross-account report/export scope, export filename/ordering ambiguity, stale scenario feedback mappings, local path exposure in review archives, report-to-action links dropping request dimensions such as `limit`, invoice/document links or routes that drop simulated viewer scope on either first-hop or second-hop actions or skip payer-row checks, member bill summaries that hide invoice IDs but still need restricted-invoice state plus scoped CUR actions, silent truncation in financial summary cards or drilldown totals, HTTP method guards returning 405 with route-specific `Allow` headers, HEAD download handlers that should preserve GET download headers while returning before CSV/PDF byte rendering or body writes, decimal form parsers that must reject `NaN`/`Inf` before scaling into micros or basis points, and payment workflows that must enforce `ViewPayments` on both GET and state-changing POSTs with hidden viewer fields preserved [17][20][21][22][24][25][26][27]. Fresh-eyes maintainability passes should also look for monolithic tests/handlers, shared helpers living in arbitrary feature files, repeated viewer-policy resolution, duplicated seed fixtures across SQL and Go, string-matched domain errors, and wide DSL structs with separate parse/validate/execute switches; when fixing string-matched domain errors, keep learner-facing text stable while adding sentinel/domain errors and tests for wrapped sentinels plus plain lookalike strings. App-wide helper additions now belong in the named shared files under `internal/app` (`http_helpers.go`, `query_helpers.go`, `format_helpers.go`, `display_helpers.go`, `viewer_helpers.go`, or `export_paths.go`) with focused coverage in `shared_helpers_test.go` before adding generic helpers to feature handlers. Schema-backed source checks can miss generic feedback fallbacks when the fallback table is real, so feedback tests should assert user-facing what-changed/concept text for every supported action and packaged scenario tests should cover authored paths. Prefer tests that prove the user-visible workflow or persistence invariant at risk, and keep mind-map updates to the reusable lesson or changed product contract, not the full issue history [14][30].
[30] **Mind-Map Maintenance** - `bd-2mx` and `bd-2jz` consolidated `MIND_MAP.md` from long per-ticket session diaries into this compact node set while preserving protected format nodes [1]-[5], restoring reusable agent-mistake coverage at [14], and aligning project/workflow anchors with `README.md` and `AGENTS.md` [6][9][15][27]. Future updates should modify the closest existing node, add a new node only for a genuinely new concept, prefer durable patterns over issue lists, and avoid repeating issue IDs or "no new mistake" notes across many nodes [1][5][14][29].
[31] **Export Visibility Enforcement** - `bd-hz2.1.4` makes CUR CSV, export reconciliation, stored export generation, listing, download, and regeneration derive `viewer_role`/`viewer_account_id` policy: management and finance are payer-scoped, members are usage-account scoped, and instructor remains all-account [17][24][29]. `bd-1r6.4.1` adds FOCUS-like CSV direct downloads and stored `focus_csv` exports over the same bill-line source rows, route policy, stored visibility metadata, and regeneration controls; `bd-1r6.4.3` adds stored `focus_metadata_json` sidecars that target FOCUS v1.4, classify FOCUS-mapped versus `x_Simulator...` columns, preserve the explicit `not_conformant` boundary, and share the CSV export visibility scope [26][27]. Member FOCUS-like exports hide payer bill IDs, invoice IDs, and payer-scoped support rows [26][27]. Member exports preserve viewer scope in links/forms, hide payer bill IDs in generated CSV metadata, and reconciliation reports compare visible line-item totals rather than consolidated bill/invoice totals; payer-scoped views retain consolidated bill/invoice comparisons [20][24][27]. Stored CUR and FOCUS-like exports persist their generation visibility scope in metadata, member-scoped filenames include usage visibility to avoid collisions with management usage-filtered exports, and member list/download/regenerate paths require usage-scope generation metadata instead of trusting `usage_account_id` alone [14][29]. Export generation and regeneration POST error pages must render from submitted form values so denied or invalid member/finance requests keep their simulated viewer controls, action links, and retry fields scoped to the posted viewer [29]. App export helpers are split by responsibility across request parsing, visibility scoping, persisted generation, file metadata/filenames, inventory views, and reconciliation views; keep future export type changes in the matching focused file instead of re-consolidating workflow helpers [29].
